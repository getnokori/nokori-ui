export enum SentMailStatuses {
  'accepted',
  'delivered',
  'queued',
  'failed'
}

export enum SentMailStatusLabels {
  'accepted' = 'Accepted',
  'delivered' = 'Delivered',
  'queued' = 'Queued',
  'failed' = 'Failed'
}
