enum SourceEngines {
  COUCHDB = 'couchdb',
  DYNAMODB = 'dynamodb',
  ELASTICSEARCH = 'elasticsearch',
  MARIADB = 'mariadb',
  MONGODB = 'mongodb',
  MYSQL = 'mysql',
  NEO4J = 'neo4j',
  POSTGRES = 'postgres',
  RABBITMQ = 'rabbitmq',
  REDIS = 'redis',
  HTTP = 'http'
}

export default SourceEngines
